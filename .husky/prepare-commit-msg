#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Skip interactive commitizen in non-TTY environments (Windows, VS Code GUI, CI)
if [ -t 0 ]; then
  exec </dev/tty && node_modules/.bin/cz --hook || true
fi
